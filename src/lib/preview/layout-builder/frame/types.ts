export type FrameOptions = {
	theme: 'light' | 'dark';
	align: 'left' | 'center' | 'right';
	model: string;
	make: string;
	focalLength: string;
	fNumber: string;
	exposureTime: string;
	iso: string;
};
